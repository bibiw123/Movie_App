export type UserRegisterDTO = {
    username: string;
    email: string;
    password: string;
}
export type UserRegisterResponseDTO = {
    username: string;
    email: string;
    password: string;
}